# Security Policy

## Scope

默认版本是静态 mock-first 应用，不接收账号、令牌、媒体文件或外部网络数据。安全问题仍可能出现在依赖、构建流程、跨站脚本或未来适配器边界。

## Reporting

请不要在公开 issue 中发布凭据、个人信息、真实作品或可复现的生产数据。优先通过仓库的 GitHub Security Advisories 私下报告；若该功能不可用，请联系维护者并提供：

- 受影响的提交或版本；
- 最小复现步骤；
- 影响范围和建议修复；
- 已采取的临时缓解措施。

我们会确认收到报告，评估影响，并在修复可公开后发布说明。请给维护者合理的修复时间，不要在补丁前公开细节。

## Maintainer checklist

- 所有 CI 权限保持最小化；
- 依赖更新通过 lockfile 审查；
- 适配器凭据只来自运行环境，不进入日志或 fixture；
- 真实数据只能留在受控环境，发布前执行秘密和媒体扫描。
- 蛙蛙面板只接受脱敏聚合快照；不得加入已认证采集器、浏览器 Profile、Cookie、账号、作品名、远端 ID 或原始统计导出。
